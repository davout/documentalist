require 'documentalist'

#Dir["#{Gem.searcher.find('facebooker').full_gem_path}/**/tasks/*.rake"].each { |ext| load ext }

# Automatically load tasks when used in Rails
#Dir.glob(File.join(File.dirname(__FILE__), %w{ .. lib tasks ** *.rb})).each do |file|
#  load file
#end
